import re
with open('js/app.js', 'r') as f:
    content = f.read()

# I also need to make sure the tooltip of the circleChartCanvas is fixed, because I replaced one shared block with the HO chart config. Did I miss the Circle Chart config?
